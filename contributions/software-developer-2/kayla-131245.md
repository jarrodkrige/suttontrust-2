# Submission from kayla

> Team 2 · Role software-developer-2 · 2026-05-08 13:12


import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { motion } from "framer-motion";

export default function BuddySignup() {
  const [submitted, setSubmitted] = useState(false);

  if (submitted) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-[#FAFAFA]">
        <Card className="p-6 rounded-2xl shadow-lg text-center">
          <CardContent>
            <h2 className="text-2xl font-bold mb-2">you're in 🎉</h2>
            <p className="text-[#6B7280]">
              we’ll match you with a buddy soon 💛
            </p>
          </CardContent>
        </Card>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-[#FAFAFA] flex items-center justify-center p-6">
      <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}>
        <Card className="rounded-2xl shadow-lg p-6 w-full max-w-md">
          <CardContent>
            <h1 className="text-2xl font-bold mb-4">
              find your buddy 🤝
            </h1>

            <p className="text-[#6B7280] mb-6">
              tell us a bit about you so we can match you with the right person
            </p>

            <div className="space-y-4">
              <Input placeholder="nickname (anonymous)" />

              <select className="w-full p-2 rounded-xl border">
                <option>what do you need?</option>
                <option>just someone to talk to</option>
                <option>advice</option>
                <option>study support</option>
              </select>

              <select className="w-full p-2 rounded-xl border">
                <option>your availability</option>
                <option>after school</option>
                <option>weekends</option>
                <option>anytime</option>
              </select>

              <select className="w-full p-2 rounded-xl border">
                <option>your role</option>
                <option>i want support</option>
                <option>i can support others</option>
              </select>

              <Button
                onClick={() => setSubmitted(true)}
                className="w-full bg-[#FF8FA3] text-white rounded-2xl py-2 hover:scale-105 transition"
              >
                find my buddy ✨
              </Button>
            </div>
          </CardContent>
        </Card>
      </motion.div>
    </div>
  );
}

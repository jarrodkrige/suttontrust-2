# Submission from kayla

> Team 2 · Role software-developer-2 · 2026-05-08 13:03


import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { motion } from "framer-motion";

export default function CharitySite() {
  const [page, setPage] = useState("home");

  return (
    <div className="min-h-screen bg-[#FAFAFA] p-6">
      {page === "home" && (
        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }}>
          <Card className="rounded-2xl shadow-lg p-6 text-center">
            <CardContent>
              <h1 className="text-3xl font-bold text-[#2F2F2F] mb-4">
                make a difference 💖
              </h1>
              <p className="text-[#6B7280] mb-6">
                find opportunities, help others, and build your future
              </p>

              {/* CALL TO ACTION BUTTON */}
              <Button
                onClick={() => setPage("cta")}
                className="bg-[#FF8FA3] text-white rounded-2xl px-6 py-3 text-lg hover:scale-105 transition"
              >
                get involved ✨
              </Button>
            </CardContent>
          </Card>
        </motion.div>
      )}

      {page === "cta" && (
        <motion.div
          initial={{ opacity: 0, y: 20 }}
          animate={{ opacity: 1, y: 0 }}
        >
          <Card className="rounded-2xl shadow-lg p-6">
            <CardContent>
              <h2 className="text-2xl font-bold mb-4">
                get involved 🌍
              </h2>

              <p className="text-[#6B7280] mb-6">
                choose how you want to make an impact:
              </p>

              <div className="grid gap-4">
                <div className="p-4 bg-[#FFC8DD] rounded-2xl">
                  🙌 find volunteering opportunities
                </div>
                <div className="p-4 bg-[#BDE0FE] rounded-2xl">
                  💬 join the community
                </div>
                <div className="p-4 bg-[#A8E6CF] rounded-2xl">
                  🎧 explore wellbeing tools
                </div>
              </div>

              <Button
                onClick={() => setPage("home")}
                variant="outline"
                className="mt-6 rounded-2xl"
              >
                ← go back
              </Button>
            </CardContent>
          </Card>
        </motion.div>
      )}
    </div>
  );
}
